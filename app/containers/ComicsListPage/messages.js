/*
 * ComicsListPage Messages
 *
 * This contains all the text for the ComicsListPage container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.ComicsListPage'

export default defineMessages({
  loadMore: {
    id: `${scope}.loadMore`,
    defaultMessage: 'Carregar mais',
  },
})
